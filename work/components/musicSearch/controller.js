// donde va la logica de negocio

// Se quiere buscar artistas por genero

function buscarPorGenero(usuario, genero){
    console.log(genero);
    console.log(usuario);
}

module.exports = { //exportamos todas las funciones que querramos
    buscarPorGenero,
};